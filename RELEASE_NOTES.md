## SolarForge v0.3.31

### 🏢 系統グループ — 建屋別のPCS設計ができるようになりました

建屋が分かれている等で「PCSを跨がせたくないパネル群」を宣言して、グループごとにPCSを選定・割付できます。

- モデル上でパネルを範囲選択 → ストリングタブの「系統グループ」でグループ名を付けます（例: A棟折板／B棟陸屋根）
- 「設計対象」でグループを選ぶと、候補検索〜設計〜割付がそのグループだけで実行されます
- PCS番号・ストリング番号は続き番号で自動採番。割付クリアもグループ単位（他グループは保持）
- グループを使わなければ従来どおり全体一括です

### 🔒 サイト全体ルール（既定のガード — 外せば従来どおり自由）

- **同一機種で統一**（既定ON）: 他のグループへ割付済みの機種で候補を縛ります。出力制御の指令は定格に対する%で各PCSへ一斉に入るため、容量が混在するとkWの刻みがバラつき、容量帯によって応答速度も異なり制御が波打つ原因になります — その均一化のためのガードです
- **AC合計上限**（任意・例 49.5）: 候補表に「サイトAC計」（割付済みグループ＋その候補）が表示され、上限超過を警告します（低圧50kW枠などの連系区分の確認用）
- 🧩PCS構成の自由選定・補助自立PCSは従来どおり使えます

### 📄 レポートの拡充

- 方位グループ表に**構成比（枚数% / 容量%）**列 — 折板×バラスト混在案件でグループの比重が一目で分かります
- ストリング構成表に系統グループ列（グループ運用時）
- 「系統グループ別PCS構成」表 — グループごとの機種・台数・AC小計・PCS番号と、AC合計・同一機種/混在の注記

### 導入

プラグイン内「⬆更新」ボタンから自動更新できます。更新後はSketchUpを再起動してください。
