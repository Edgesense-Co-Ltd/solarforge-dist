# SolarForge v0.3.38（2026-09-16・テストユーザー向け）

傾斜架台を配置後に削除するとSketchUp本体が終了するとの報告に対する、安全対策版です。

- 生成済みの架台を重ねてグループ化する処理を廃止。
- 「直前の配置をやり直し」で、編集中の対象や別モデルの配置を削除しないよう保護。
- 撤去失敗時に成功扱いせず、理由を画面に表示。
- v0.3.37までの配置・ストリング・デマンド・PCS関連の変更を含みます。

**自動テストは成功していますが、SketchUp実機での削除クラッシュ解消は未確認です。** 更新前にモデルを保存し、新規モデルまたはコピーで確認してください。既に作成した問題モデルの構造を自動修復するものではありません。

導入: SolarForge-0.3.38.rbzを拡張機能マネージャでインストールし、SketchUpを再起動。
確認: 起動ログのv0.3.38を確認し、テスト用モデルで配置→削除→Undoを確認。再発した場合は作業を止め、削除方法（Deleteキー／右クリック／やり直しボタン）とBugSplat番号をお知らせください。

RBZ: 661,593 bytes / SHA-256: 21235968c76be866c25c78651c02e2f2a943132f2ae8a5b5b4af57176552b5e7
ソース: Edgesense-Co-Ltd/solarforge PR #84 / 966612a7590d465b6f8913763fd2d2b94cd7bdc5（ドラフト・実機未検証）

---

# v0.3.37 — デマンドの月別シート選択・計算用出力上限（テストユーザー実機確認用）

- デマンドのシート欄を常時表示し、自動判定候補がないシートも選択可能にしました。選択シートを対象にAI再判定でき、古い応答が別のシートへ戻すことを防ぎます。
- 発電タブに「合計出力上限（計算用）」を追加。例: 定格50＋4.95kWに対し49.95kWを指定すると、合計時別発電量をその上限で計算し、ピークカット・年間/月別・CSV/BESS用系列・帳票に反映します。空欄は追加制限なし。設定はモデル別に保存します。
- PCS定格と計算用上限を分けて表示します。実機への制御指令、PCS定格の変更、連系区分の判定を行う機能ではありません。実機の出力制限設定可否は未検証です。
- 過積載率が空欄の際に黙って1.6を使うのを止めました。候補0件のときの絞り込み理由と実際に使った率を表示します。自由構成は台数固定で、通常選定でもDC入力上限やMPPT回路数によって台数が減らない場合があります。
- 候補表のサイト定格AC合計に補助PCS分を加算。50＋4.95kWを54.95kWとして表示します。
- v0.3.35/0.3.36のパネル縦隙間・傾斜架台・ストリング/PCSの建屋屋根境界修正を含みます。

自動テストで確認した実機検証用の先行配布です。SketchUp2024および報告元Excelの実データでは未確認。発電量の上限が49.95kWでも、元のピークが33.97kWなど上限未満なら追加カットは発生しません。ソースPR #80〜#83は独立実機検証待ちのドラフトです。

## SolarForge v0.3.36

テストユーザー向けの実機確認用配布です。v0.3.35の配置修正も含みます。

### 建屋・屋根をまたぐストリング割付を修正

- 通常のストリング自動割付とPCS設計の両方で、屋根・建屋の配置グループを境界にします。同じ方位・傾斜、同じ屋根名でも別々に扱います。
- 屋根ごとの端数を別屋根のパネルで補わず、端数ストリングを個別に表示します。従来から系統数・必要MPPT数が増える場合があります。
- 古い屋根構成の計画は再計算を案内します。建屋コンポーネントの複製で内部パネルを共有している場合は「固有にする」で分離してから設計してください。
- グループを分解したパネルは所属屋根を推測しません。屋根ごとにグループ化してください。

### 更新・確認方法

`SolarForge-0.3.36.rbz` を拡張機能マネージャから上書きインストールし、SketchUpを再起動してください。
既存モデルはインストールだけでは書き換わりません。確認用コピーでPCS設計を再計算し、割付をやり直してください。
各ストリングが1つの屋根に収まり、直列数・MPPT数・端数が表示と一致することを確認してください。

PCS設計後に通常のストリング自動割付を実行すると、従来どおりPCS/MPPTの割付がクリアされます。PCS設計を使う場合はPCS側の設計・割付を実行してください。

自動テストは成功。SketchUp実機での結果はこれから確認します。
ソース: `8be9a0780f6867a2b20b19512cb2b1eed9f36455`
SHA-256: `c4b4316160bf2ac526f918524e824182d323e020560203df96bdee5f8e3d1ba8`

---

## SolarForge v0.3.35

テストユーザー向けの実機確認用配布です。自動テストは成功しています。SketchUp実機での確認結果はこれから収集します。

### 修正内容

- 選択面への傾斜架台配置で、指定した方位を反映するようにしました（180°=南、0°=北）。
- チルトは水平基準の角度になりました。設置面が10°でも、10°指定ならパネルのチルトは10°です。
- 2段のパネルを同じ傾斜面上へ並べます。段内行間はパネル面に沿った隙間、アレイ間寸法は水平クリア距離です。
- 同型番のパネル寸法をDBで変更した後、新規配置に古い形状が使われて縦隙間が広がる不整合を修正しました。
- ハゼ金具の通常Y隙間は15〜40mm。追加チルトは0°を指定してください（屋根勾配は選択面から取得）。点検通路幅は別設定です。
- 横方向のハゼ合わせによる間隔調整は従来どおりです。

### 更新・確認方法

`SolarForge-0.3.35.rbz` をSketchUpの拡張機能マネージャから上書きインストールし、SketchUpを再起動してください。
既存配置は自動では変わらないため、確認用モデルで新たに配置してください。

まず「2段・方位180°・チルト10°」で、南向き・水平基準10°・同一面上の2段配置を確認してください。
縦隙間はパネル面に沿って測定してください。うまくいかない場合は設定値と配置画像、直近ログをお知らせください。

ビルド元: `ea7e589b177b77b76905efa46248e172f9e1dab9`
SHA-256: `c73bbe6e584cfff662b096b00fba0f278199745807a687e13c9df5db431ae956`

---

## SolarForge v0.3.34

### 🔐 AI 機能の呼び出しにライセンスキーを添えるようにしました（機能追加なし）

- AI 相談・AI 提案などの呼び出しに、お手元のライセンスキーを自動で添えて送るようにしました。操作は変わりません。
- 近日、サーバー側で「ライセンスキーのある呼び出しだけを受け付ける」設定に切り替えます。切り替え後は **v0.3.34 より前の版では AI 機能が使えなくなります**。お早めに更新してください。
- 起動直後でキーがまだ読み込まれていない場合は、これまでどおりキー無しで送ります（自動で復帰します）。

### 更新方法
`SolarForge-0.3.34.rbz` を上書きインストール → SketchUp を再起動。

---

## SolarForge v0.3.33

### ✅ 設計チェック — 出図前に「抜け」と「不整合」を自動で洗い出します

出力タブに「✅ 設計チェック」を追加しました。レポートに出る数字を**逆から検算**し、
見落としがちな項目を一覧にします。各項目には根拠の数値と「次に何をするか」が付きます。

**抜けの検出**
- **積雪地域なのに積雪シミュレーションが未実行**（最寄り観測地点の最大積雪深20cm以上で判定）—
  発電量の目減りと、架台・金具の検討根拠の両方が欠落することを指摘します
- 発電シミュ未実行／影解析未実施／ストリング未割付／デマンド取込済みだが発電量と突合できていない

**数字の整合（レポートからの逆算）**
- **シミュ実行後にパネルを増減してDC容量がズレている**（＝レポートの発電量が古い）
- 比発電量が妥当域（1000〜1300kWh/kW目安）から外れている — 低い/高いで原因候補も提示
- 年間発電量と時別8760の合計が合わない（見積Excelとの数字ズレの原因）
- **実際の割付の直列数が設計範囲から外れている** — 上振れは低温時の過電圧、下振れはMPPT下限割れ

**積雪・構造（「金具少なくて大丈夫か」への回答）**
- **積雪時の金具1本あたりの荷重**を実数で提示します（ピーク積雪荷重×パネル面積＋自重÷支持点数）。
  内訳も開示するので、そのまま金具メーカーの許容支持荷重と照合できます
- パネル1枚あたりの金具本数が4点支持を下回っている場合に指摘（積雪地では「要修正」）
- パネルDBに金具設置可能範囲（S寸法）が未登録の場合も指摘

> 金具の許容支持荷重は機種によって幅があり本ツールは保有していません。そのため**断定はせず、
> 数値を提示して「要確認」**とします。また算出した積雪荷重は気象の平年値ベースであり、
> 建築確認で用いる設計用垂直積雪量とは別物である旨も明記しています。

### 📄 レポートにも同梱

レポート末尾に「設計チェック（社内確認用）」として同じ内容を出力できます
（出力タブのチェックでON/OFF可・既定ON）。

### 導入

プラグイン内「⬆更新」ボタンから自動更新できます。更新後はSketchUpを再起動してください。
